name: Check API Health

on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Check Health of Api
        run: |
          curl -f https://nutrino.onrender.com/docs
          curl -f https://nutrino-web.onrender.com/